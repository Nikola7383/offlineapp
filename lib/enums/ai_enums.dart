enum AIServiceType {
  predictiveThreatAnalysis,
  selfHealing,
  enhancedThreatDetection,
  stealthMaster,
  secureBroadcast,
  phoenixProtocol,
  secretReporting,
  autonomousSecurity,
  offlineAI
}

enum AIProcessingState {
  idle,
  initializing,
  processing,
  analyzing,
  learning,
  adapting,
  completed,
  error
}

enum AIConfidenceLevel { veryLow, low, medium, high, veryHigh, certain }

enum AIThreatLevel { none, low, moderate, high, critical, unknown }

enum AIResponsePriority { lowest, low, normal, high, urgent, immediate }

enum AIOperatingMode {
  training,
  evaluation,
  production,
  maintenance,
  debug,
  offline
}

enum AILearningStrategy {
  supervised,
  unsupervised,
  reinforcement,
  transfer,
  federated,
  hybrid
}

enum AIDataSource {
  realTime,
  historical,
  synthetic,
  hybrid,
  external,
  internal
}

enum AIOptimizationTarget {
  speed,
  accuracy,
  efficiency,
  reliability,
  resourceUsage,
  balanced
}
